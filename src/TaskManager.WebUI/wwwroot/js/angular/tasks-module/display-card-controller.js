(function (angular) {
    "use strict";

    angular.module("tasks-module")
    .controller("DisplayCardCtrl", ["$scope", "SweetAlert", "$http", "taskRunner", "constants", "page",
        function ($scope, sweetAlert, $http, taskRunner, constants, page) {
       
        }]);
})(angular);